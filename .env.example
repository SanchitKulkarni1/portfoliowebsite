# Base URL of the Career Graph API (backend/), no trailing slash.
# Without it the site still works; the chat shows an "offline" state and the graph renders from bundled data.
VITE_CAREER_GRAPH_API_URL=http://localhost:8000
